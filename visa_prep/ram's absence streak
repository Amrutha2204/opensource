n = int(input())
att = list(map(int, input().split()))
maxi = current = 0
for day in att:
    if day == 0:
        current += 1
        maxi = max(maxi, current)
    else:
        current = 0
print(maxi)
